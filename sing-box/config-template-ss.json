{
    "inbounds": [
      {
        "type": "{{PROTOCOL}}",
        "listen": "::",
        "listen_port": {{PORT}},
        "network": "tcp",
        "method": "{{METHOD}}",
        "password": "{{PASS}}"
      }
    ],
    "outbounds": [
        {
          "type": "{{PROTOCOL}}",
          "tag": "outline-out",
          "server": "{{SERVER}}",
          "server_port": {{PORT}},
          "method": "{{METHOD}}",
          "password": "{{PASS}}"
        }    
    ]
}
