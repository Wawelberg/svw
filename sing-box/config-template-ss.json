{
    "inbounds": [
      {
        "type": "{{PROTOCOL}}",
        "listen": "::",
        "listen_port": {{PORT}},
        "network": "tcp",
        "method": "{{METHOD}}",
        "password": "{{PASS}}"
      }
    ]
}
